<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Voter Details</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            background: linear-gradient(to right, #e3f2fd, #bbdefb);
            font-family: 'Arial', sans-serif;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }
        .nav-bar {
            background: linear-gradient(to right, #00796b, #004d40);
            padding: 15px;
            text-align: center;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
        }
        .nav-bar a {
            color: white;
            font-size: 1.2rem;
            font-weight: bold;
            text-decoration: none;
            padding: 10px 15px;
            transition: color 0.3s ease-in-out;
        }
        .nav-bar a:hover {
            color: #bbdefb;
        }
        .container-box {
            max-width: 700px;
            background: white;
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.3);
            transition: transform 0.3s ease-in-out, box-shadow 0.3s ease-in-out;
            text-align: center;
            margin: auto;
        }
        .container-box:hover {
            transform: scale(1.03);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.4);
        }
        .photo {
            width: 200px;
            height: 200px;
            border-radius: 50%;
            object-fit: cover;
            margin: 20px auto;
            display: block;
            border: 4px solid #00796b;
        }
        .details p {
            font-size: 1.2rem;
            color: #333;
            margin-bottom: 8px;
            text-align: left;
        }
        .footer {
            background: linear-gradient(to right, #00796b, #004d40);
            color: white;
            text-align: center;
            padding: 10px;
            margin-top: auto;
        }
        nav {
            background: linear-gradient(to right, #00796b, #004d40);
            padding: 16px 0;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
            position: sticky;
            top: 0;
            z-index: 1000;
        }
        nav a {
            color: white;
            font-weight: 700;
            font-size: 1.2rem;
            padding: 12px 18px;
            transition: color 0.3s, transform 0.3s;
            text-decoration: none;
        }
        nav a:hover {
            transform: scale(1.15);
            color: #c8e6c9;
        }
    </style>
</head>
<body class="bg-gray-100 font-sans">
    <header class="bg-gradient-to-r from-teal-700 to-green-500 text-white py-6 text-center text-4xl font-bold shadow-md">
        Secure E-Voting System
    </header>

<!-- Navbar -->
<nav class="sticky top-0 z-50">
    <div class="container mx-auto flex justify-center space-x-8 text-xl">
        <a href="about">About</a>
        <a href="votedetails">user details</a>
        <a href="auth">Authentication Process</a>
        <a href="decrypto">Voter Registration</a>
        <a href="vote">Vote Casting</a>
        <a href="/">Logout</a>
    </div>
</nav>

<!-- Main Content -->
<div class="flex items-center justify-center min-h-screen">
    <div class="container-box">
        <h2 class="text-center font-bold text-2xl mb-4">Voter Details</h2>
        <img id="photo" class="photo">
        <div class="details text-left">
            <p><strong>Name:</strong> <span id="name"></span></p>
            <p><strong>Age:</strong> <span id="age"></span></p>
            <p><strong>Country:</strong> <span id="country"></span></p>
            <p><strong>State:</strong> <span id="state"></span></p>
            <p><strong>Constituency:</strong> <span id="constituency"></span></p>
            <p><strong>Email:</strong> <span id="email"></span></p>
            <p><strong>Voter ID:</strong> <span id="voterId"></span></p>
        </div>
    </div>
</div>

<!-- Footer -->
<div class="footer">
    <p>© 2025 Secure E-Voting System | All Rights Reserved</p>
</div>

<script>
    document.getElementById("name").innerText = localStorage.getItem("name");
    document.getElementById("age").innerText = localStorage.getItem("age");
    document.getElementById("country").innerText = localStorage.getItem("country");
    document.getElementById("state").innerText = localStorage.getItem("state");
    document.getElementById("constituency").innerText = localStorage.getItem("constituency");
    document.getElementById("email").innerText = localStorage.getItem("email");
    document.getElementById("voterId").innerText = localStorage.getItem("voterId");
    document.getElementById("photo").src = localStorage.getItem("photo");
</script>

</body>
</html>
