<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Secure E-Voting System</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        body {
            scroll-behavior: smooth;
            background: linear-gradient(to right, #e3f2fd, #bbdefb);
        }
        nav {
            background: linear-gradient(to right, #00796b, #004d40);
            padding: 12px 0;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
        }
        nav a {
            color: white;
            font-weight: 600;
            padding: 10px 15px;
            transition: color 0.3s, transform 0.2s;
        }
        nav a:hover {
            transform: scale(1.1);
            color: #c8e6c9;
        }
    </style>
</head>
<body class="bg-gray-100 font-sans">
    <header class="bg-gradient-to-r from-teal-700 to-green-500 text-white py-6 text-center text-4xl font-bold shadow-md">
        Secure E-Voting System
    </header>

    <nav class="sticky top-0 z-50">
        <div class="container mx-auto flex justify-center space-x-8 text-xl">
            <a href="/about">About</a>
            <a href="/votedetails">User Details</a>
            <a href="/auth">Authentication Process</a>
            <a href="/signup">Voter Registration</a>
            <a href="/vote">Vote Casting</a>
            <a href="mailto:venkatkarthikeyandec@gmail.com">Contact</a>
        </div>
    </nav>

    <section class="container mx-auto p-8 section fade-in" id="registration">
        <div class="grid md:grid-cols-2 gap-8 items-center">
            <div>
                <h2 class="text-3xl font-semibold text-gray-800">Why Choose Our E-Voting System?</h2>
                <p class="mt-4 text-gray-600 leading-relaxed">A secure and efficient voting process leveraging QR-code authentication and visual cryptography.</p>
                <ul class="mt-4 space-y-3">
                    <li class="flex items-center text-lg"><span class="text-teal-500 text-2xl mr-2">✔</span> QR-Code Authentication</li>
                    <li class="flex items-center text-lg"><span class="text-teal-500 text-2xl mr-2">✔</span> Visual Cryptography</li>
                    <li class="flex items-center text-lg"><span class="text-teal-500 text-2xl mr-2">✔</span> Real-Time Vote Counting</li>
                    <li class="flex items-center text-lg"><span class="text-teal-500 text-2xl mr-2">✔</span> Blockchain Integration</li>
                    <li class="flex items-center text-lg"><span class="text-teal-500 text-2xl mr-2">✔</span> Multi-Device Support</li>
                    <li class="flex items-center text-lg"><span class="text-teal-500 text-2xl mr-2">✔</span> Scalable and Secure</li>
                </ul>
                <button class="mt-6 px-6 py-3 bg-teal-700 text-white text-lg rounded-lg shadow-md btn" onclick="openLoginModal()">Get Started</button>
            </div>
            <div>
                <img src="pictures/vote.png" alt="QR Code Voting" class="rounded-lg shadow-xl">
            </div>
        </div>
    </section>

    <section class="container mx-auto p-8 section fade-in" id="about">
        <h2 class="text-3xl font-semibold text-gray-800 text-center">About Our E-Voting System</h2>
        <p class="mt-4 text-gray-600 text-center leading-relaxed">Our secure e-voting system ensures transparency and reliability in elections. By leveraging QR-code authentication and visual cryptography, we provide a safe and scalable voting solution.</p>
    </section>

    <div id="loginModal" class="modal fade" tabindex="-1" role="dialog">
        <div class="modal-dialog" role="document">
            <div class="modal-content p-4">
                <h5 class="modal-title">Login</h5>
                <div class="modal-body">
                    <form id="loginForm" action="/login" method="POST">
                        <input type="email" name="email" id="username" class="form-control mb-3" placeholder="Email" required>
                        <input type="password" name="password" id="password" class="form-control mb-3" placeholder="Password" required>
                        <button type="submit" class="btn btn-primary w-100">Login</button>
                        <div class="text-center mt-2">
                            <a href="/signup">Create New Account</a> | <a href="/forgot-password">Forgot Password?</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <div id="registerModal" class="modal fade" tabindex="-1" role="dialog">
        <div class="modal-dialog" role="document">
            <div class="modal-content p-4">
                <h5 class="modal-title">Register</h5>
                <div class="modal-body">
                    <input type="text" id="newUsername" class="form-control mb-3" placeholder="Username">
                    <input type="password" id="newPassword" class="form-control mb-3" placeholder="Password">
                    <button class="btn btn-primary w-100" onclick="registerUser()">Register</button>
                </div>
            </div>
        </div>
    </div>

    <footer class="bg-gradient-to-r from-teal-700 to-green-500 text-white text-center py-6 mt-6 text-lg">
        &copy; 2025 Secure E-Voting System | Ensuring Safe and Transparent Elections
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        function openLoginModal() {
            var loginModal = new bootstrap.Modal(document.getElementById('loginModal'));
            loginModal.show();
        }
    
        function openRegisterModal() {
            var registerModal = new bootstrap.Modal(document.getElementById('registerModal'));
            registerModal.show();
        }
    
        function registerUser() {
            const username = document.getElementById('newUsername').value;
            const password = document.getElementById('newPassword').value;
            
            if (username && password) {
                window.location.href = '/signup';
            } else {
                alert('Please fill in all fields');
            }
        }
    
        function forgotPassword() {
            window.location.href = '/forgot-password';
        }
    </script>
</body>
</html>